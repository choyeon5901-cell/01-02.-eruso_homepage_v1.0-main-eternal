 커밋 전후 처리 순서 (최종)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 1단계: Git 초기화 및 커밋
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

cd /workspaces/default/code/04-01.-eruso_eternal_memory

git init
git add .
git commit -m "feat: Add memorial template system

- Add 4 template designs (Classic, Modern, Garden, Minimalist)
- Add template selection UI in memorial detail page
- Add backend API support for template field
- Add SQL migration for template column"

git remote add origin https://github.com/사용자명/저장소명.git
git remote add origin https://github.com/choyeon5901-cell/01-02.-eruso_homepage_v1.0-main-eternal.git
git push -u origin main

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 2단계: 서버 배포 (코드 반영)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 서버에 접속
ssh user@your-server

# 코드 pull
cd /path/to/project
git pull origin main

# Backend 재시작
pm2 restart backend

# Frontend 빌드 (필요시)
cd frontend
npm run build

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 3단계: DB 마이그레이션 (마지막)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

mysql -u root -p eternal_memory < docs/ETERNAL_template_컬럼추가.sql

# 또는 MySQL 클라이언트에서 직접:
# ALTER TABLE memorial_rooms ADD COLUMN template VARCHAR(20) DEFAULT 'classic' NOT NULL;

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 4단계: 확인
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# DB 확인
mysql -u root -p eternal_memory -e "DESCRIBE memorial_rooms"

# 브라우저에서 확인
# http://your-server:3000/memorial/1
# → "템플릿 선택" 탭 클릭
# → 템플릿 선택 후 "선택한 템플릿 적용" 클릭
# → 화면이 선택한 템플릿으로 바뀌는지 확인


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━



좋습니다! 정상 업로드 축하드립니다 🎉

📋 확인해야 할 파일 목록
1️⃣ 템플릿 컴포넌트 (4개)
frontend/src/components/templates/
├── ClassicTemplate.js       ← 보라색 우아한 디자인
├── ModernTemplate.js        ← 검은색 모던한 디자인
├── GardenTemplate.js        ← 초록색 자연 테마
├── MinimalistTemplate.js    ← 흰색 미니멀 디자인
└── templates.css            ← 모든 템플릿 스타일
2️⃣ Backend 수정 파일 (3개)
backend/app/
├── models/memorial_room.py              ← 15번 줄: template 필드 확인
├── schemas/memorial_room.py             ← 8번, 14번 줄: template 확인
└── api/memorial_room.py                 ← 33번, 61번, 243번, 273-277번 줄 확인
확인 포인트:

memorial_room.py 모델: template = Column(String(20), default='classic')
schemas/memorial_room.py: template: str | None = 'classic'
api/memorial_room.py serialize_room: "template": getattr(room, 'template', 'classic')
3️⃣ Frontend 수정 파일 (2개)
frontend/src/
├── api/memorialRoomApi.js               ← 28-30번 줄: template 전송 확인
└── pages/MemorialDetail.js              ← 37-41번, 713-726번 줄 확인
확인 포인트:

memorialRoomApi.js: if (data.template) { formData.append("template", data.template); }
MemorialDetail.js: 템플릿 선택 버튼 클릭 시 updateRoomWithAuthentication 호출
4️⃣ DB 마이그레이션
docs/ETERNAL_template_컬럼추가.sql       ← 실행 필요!
🔍 빠른 확인 명령어
# Windows PowerShell에서
cd "C:\GitHub\04-01. eruso_eternal_memory"

# 1. 템플릿 파일 존재 확인
dir frontend\src\components\templates\*.js

# 2. Backend template 필드 확인
findstr /n "template" backend\app\models\memorial_room.py

# 3. Frontend template 전송 확인
findstr /n "template" frontend\src\api\memorialRoomApi.js

# 4. SQL 파일 확인
type docs\ETERNAL_template_컬럼추가.sql
⚠️ 반드시 실행해야 할 것
DB 마이그레이션 (아직 안 했으면):

-- MySQL 접속 후
mysql -u root -p eternal_memory

-- SQL 실행
source C:\GitHub\04-01. eruso_eternal_memory\docs\ETERNAL_template_컬럼추가.sql;

-- 또는 직접 실행
ALTER TABLE memorial_rooms
ADD COLUMN template VARCHAR(20) DEFAULT 'classic' NOT NULL;